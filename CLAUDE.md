# Problem Sheet Validation Gate — 검증 게이트 구현 시 참고

7단계(저장 시점 검증 게이트)에서 막아야 할 패턴 목록.
문제 시트 생성/수정 API(`POST/PATCH /api/naesin/problems`)에서 저장 전 검증.

## 검증 규칙

### 1. 원형숫자 자동 변환
- ①②③④⑤⑥⑦⑧⑨⑩ → 1,2,3,...10 자동 변환
- ⓐⓑⓒⓓⓔⓕⓖⓗⓘⓙ → 옵션 번호로 자동 변환 (옵션이 ⓐ로 시작하면 해당 인덱스)
- `questions[].answer` + `answer_key[]` 모두 대상

### 2. 빈 정답 저장 거부
- `questions[].answer`가 빈 문자열이면 저장 거부 (외부지문 category 제외)
- `subParts[].answer`도 동일 적용

### 3. 객관식 정답 범위 검증
- options가 있으면 answer는 1~options.length 범위의 숫자여야 함
- 복수정답("1, 3")인 경우 각 숫자 모두 범위 내

### 4. 텍스트 정답 + 선택지 불일치 검증
- options가 있는데 answer가 텍스트인 경우: options 중 하나와 일치하면 자동 번호 변환,
  불일치하면 저장 차단. 근본 원인: 보기(word bank)를 options에 넣으면 라디오 UI가 됨.

### 5. "번호+텍스트" 정답 자동 정규화
- "2 She goes..." 형태(숫자+공백+대문자)면 앞의 숫자만 자동 추출 (자동 수정)

### 6. 콤마 누락 복수정답
- 정답이 범위 밖 연속숫자("45")이고 각 자릿수가 모두 유효 선지면 쉼표 삽입 ("4, 5").
  통째 숫자가 범위 안이면 미발동("12"=12번 오인 방지), 0 포함 미발동.

### 7. 서술형 전환 시 답안 형식 안내(※) 없으면 경고 (NO_FORMAT_HINT — 차단 아님)

### 8. 이미지 참조 문항 자동 삭제
- 인라인 설명 없는 "다음 그림을 보고" 문항은 저장 시 자동 삭제 + 번호 재정리

## 진행 상황 — ✅ 전부 완료 (2026-06-12 종결)

- sanitizeQuestions + validateBeforeSave 구현, normalize-answer.ts 강화
- 전수 조사: 중1~중3 문법 템플릿 387시트 25,000+문항, 정답 오류 144건 수정, 재채점 69건 상승
- ⚠️ **1598021: 저장 경로에서 validateBeforeSave 제거** — PDF/템플릿 원본을 고칠 수 없어
  차단이 import 실패만 유발. sanitizeQuestions(자동 정규화)만 유지. 재추가 금지.
- 상세 이력은 git log 2026-05-31~06-12 참고.

---

# 학생 UX 안정화 (2026-06-08~10) — 재발 방지 정리

상세 커밋 서사는 git log 참고 (db22e53~9911346 등). 남길 것은 표와 규칙만.

| 문제 | 일회성 수정 | 시스템적 재발 방지 |
|------|:-:|:-:|
| 네트워크 유실 | - | fetchWithToast retry 내장 |
| 100문항 중간 유실 | - | CHUNK_SIZE=25 자동 저장 |
| 유니코드 오채점 | - | normalize-answer.ts 정규화 |
| 수동태 채점 오류 | 특정 시트 수정 | subParts fallback + 자동 subParts 생성 |
| 타이포그래피 문자 | - | 저장 시 자동 변환 |
| 테이블 깨짐 | - | FormattedText 테이블 파싱 |
| 복합 답안 subParts 누락 | - | sanitizeQuestions 자동 분할 |
| 스테이지 완료 미반영 | - | handleStageComplete 로컬 상태 갱신 |
| 이미지 없는 그림 문항 | 93문항 삭제 + 재채점 | sanitizeQuestions 자동 삭제 |
| 콤마 누락 복수정답 ("45") | 시트 5건 수정 | sanitizeQuestions Rule 6 + 추출 규칙 |
| 선지 뭉침/손상 (ⓐⓑ) | 시트 4 + 템플릿 1 복원 | 추출 규칙 (보기 한 칸에 하나씩) |
| 서술형 정답=완전한 문장 안내 누락 | (자동탐지 불가) | 추출 프롬프트 forward 규칙 |

### 검증 규칙 추가 메모
- answer_key 형식 통일 필요: 일부 시트는 string[], 일부는 {answer, explanation}[]
- multi-select 답안(q.answer 배열)은 저장 시 answer_key에도 자동 동기화 필요
- 시트 추가 시 해당 유닛의 학생 진도(problem_completed 등)를 false로 리셋 필요

---

# 콘텐츠 헬스 스캐너 원칙 (2026-06-12 확정)

라이브 콘텐츠 전수 스캔(`validateBeforeSave` 로직)으로 학생 대면 오채점 탐지·수정 완료.
학생 시트는 MCQ_RANGE·TEXT_NOT_IN_OPTIONS·EMPTY_OPTION·EMPTY_ANSWER 전부 0 달성.

**향후 `runContentScan` 원칙** (재작업 시 필수 준수):
- 검사를 **correctness(오탐0, 🔴) vs quality(기본 숨김, 🟡)** 두 통으로 분리 —
  "🔴=무조건 진짜 버그" 신뢰 유지가 핵심
- **sanitize→validate 순서**로 돌릴 것 — 스캔이 sanitize를 안 거치면 원형숫자 정답("③")
  같은 가짜 경보가 과보고됨 (166건 중 160건이 가짜였던 사례)
- **NO_FORMAT_HINT(2,484)·NO_EXPLANATION(10,169)은 quality로 제외** — 채점 무영향.
  해설 블랭킷 AI 생성 비추(오정보 주입 위험). **BLANK_FULL_SENTENCE 검사 재추가 금지**(노이즈,
  한국어 지시문 변주로 자동 검사 불가 — 4라운드 검증됨. 추출 프롬프트 forward 규칙으로 처리).
- EMPTY_OPTION은 "빈 보기"만이 아니라 정답까지 틀린 경우가 많음 — 옵션 복원 시 반드시
  다시 풀어 정답 검증. 위치기반 답안배열 때문에 문항 삭제보다 제자리 수정이 안전.

---

# 리팩토링 백로그 (2026-07-05 전수 스캔)

- [x] ①~③ 완료: 미사용 의존성 제거 / API 라우트 createApiHandler 통일·403 정리 /
  extract-pdf 라우트 분리 (18aa7dc). ⚠️ "주석 죽은 코드" 보고는 오탐이었음 — 삭제류는 직접 확인.
- [ ] ④ naesin↔voca 학습 컴포넌트 중복 통합 (플래시카드/퀴즈/스펠링 훅 공유)
  ⚠️ 잘 돌아가는 학생 화면이므로 해당 화면 수정할 일 생길 때 같이. 급하지 않음.
- wrong-answer-detail-panel.tsx(655줄) 분리는 여유 있을 때. 나머지 대형 컴포넌트는 "그대로 둠" 판정.
- my-report/parent-share는 토큰 기반 특수 인증이라 수동 라우트 유지 (정당).

---

# select('*') → 컬럼 명시 전환 (egress 절감) — ⏸️ 보류 (2026-07-05)

**보류 결정**: 과거 유령 컬럼 사고로 기존 기능 다수 파손 이력. 재개 시 반드시:
스키마 먼저 조회(추측 금지) → 테이블 하나씩 커밋 → `check:columns`+테스트+화면 확인.
ESLint `supabase/no-select-wildcard` 규칙 적용 중 (warn, 117건).

## ⚠️ 유령 컬럼 주의 — 이 리팩터의 반복 사고
`select('*')`를 명시 컬럼으로 바꿀 때 실제 스키마에 없는 컬럼을 적으면 쿼리가 통째로
에러나 빈 목록으로 조용히 망가짐. 사고 이력 20건 (naesin_textbooks.cover_image_url 등).
- 흔한 함정: 시도/진도 테이블엔 `unit_id`가 없고 `sheet_id`/`lesson_id`/`quiz_set_id`만
  있음 → 조인으로 유도. 영상진도 시각은 `created_at`이 아니라 `updated_at`.
- **재발 방지**: `npm run check:columns` — PostgREST 스키마와 코드 대조, build 체인 포함.
  컬럼 추가/변경 후 반드시 한 번 돌릴 것.
- 컬럼 상수: `SHEET_LITE_COLUMNS`, `SHEET_ADMIN_LITE_COLUMNS`, `PROGRESS_SUMMARY_COLUMNS`
  (src/types/naesin.ts). 우선순위: naesin_student_progress → 콘텐츠 JSONB 테이블 → 보카.

---

# 인프라 메모 (완료 작업 요약)

- **Supabase GRANT** (6-12 완료, 088): 전 71테이블 명시 GRANT. 새 테이블 생성 시 GRANT 포함 습관.
- **서버 캐시** (6-05, 381ebf4): `cached()` 래퍼(src/lib/cache/) + TTL 프리셋 + 무효화 헬퍼.
  mutation 라우트는 invalidate 연결 필수. 고빈도 GET 4개 캐시 적용, React Query 전환 4컴포넌트.
- **Vercel**: 프리뷰 환경변수 등록됨(8-02). db-push는 production 빌드에서만 실행
  (프리뷰가 실 DB에 마이그레이션 미는 사고 방지). check-env.mjs가 빌드 앞에서 필수
  NEXT_PUBLIC 변수를 검사해 실패 원인을 로그에 한국어로 출력.
- **구 홈페이지 잔재**: /curriculum/* → /curriculum 301 (Search Console 404 회수, c615894).

---

# 올킬보카 상품화 로그 (2026-07-16 ~ 08-08, 압축)

내부 도구 → 외부 판매 제품 전환기. 상세는 git log.

## 어휘 레벨 진단 (v1→v3 진화)
- **퍼널**: 가입 없이 /level-test 완주 → 결과 직전 연락처 게이트 → 익명 완주도
  voca_diagnostic_leads에 저장 (boss 리드 페이지). 가입하면 linked_student_id 연결.
- **보안**: 정답 봉인 토큰 + 서버 스테어케이스 재계산 (861b415) — 정답 노출·위조 제출 차단.
- **판정**: 스테어케이스 (10문항 라운드, 누적 ≥80% 위로 / ≤50% 아래로, 확정은 밴드 누적
  20문항 이상). **배정 규칙 (8-05 사장님 확정): "교재 단어의 20~50%를 모를 때 그 교재 배정"**
  — 측정은 50~80 확정, 확정 밴드 정답률 60% 이상이면 추천만 한 칸 상향(RECOMMEND_UP_PERCENT).
  문턱을 40~60으로 좁히면 20문항 노이즈(±11%p)와 겹쳐 불안정 → 측정/추천 분리가 정답.
- **결과 프레임**: "○○ 수준" 학년 주장 대신 "지금 시작할 교재" 라인업 (7-23 확정).
  교재는 title로 매칭 — 이름 바꾸면 밴드/라인업에서 빠짐 (diagnostic-bands + lineup 동시 수정).
- **문항**: 타이핑+5지선다 적응형 혼합 (96ab734), 숙어 제외, 상위 밴드 기초 빈도어 제외.
- **초등**: "초등 필수 영어단어 800" 시딩(33 Day) → L0 단독. 천일문 스타트는 L1로
  (초등 800과 51% 겹침 — 초등~중1 브릿지). 해커스 어원은 L6 병렬 (고1·2·3 모고 24~25% 균등 겹침).

## 2회독 시험 체계
- 유의어·반의어 시험 (round2-exam.ts 코어 + UI): 정답은 "플래시카드에서 보여준 그 값"만
  (synonyms/antonyms 필드). 1회독/2회독 유형 구분 저장·배지·분리 집계.
- Day 화면 1·2회독 완전 개방 (05a20dc) — 학생 탭 자유 전환. 관리자 '2회독' 토글 = 1회독 면제.
- 시험 강도: 학생별 합격선·제한시간·오답 재시험 + 학원 기본값. 제한시간은 글자 수 비례 자동 연장.

## 랜딩·브랜드
- **올킬보카 문법 (7-27 확정)**: 흰 바탕 + 구글 4색 포인트(파랑 도배 금지), 큰 CTA는 노랑 pill,
  제목만 GmarketSans, 배지·이모지 남발 금지. 커리큘럼·선생님·메인·헤더·후기 전면 적용.
- Pretendard 자체 호스팅 + 홍보 팔레트 공용화(google-palette.ts). /about 폐지 → 랜딩 이관.
- 무료 체험: 가입 7일 보카 전체 무료 → 이후 3 Day (내신은 1단원 체험 — 의도된 freemium).

## 주의할 함정 (이 시기에 배운 것)
- **`.in()` URL 한계**: 진도 조회를 day_id 수백 개 .in()으로 하면 URL 초과로 조용히 실패해
  전 학생 완료 표시가 사라짐 (06e256b) → 학생 스코프 조회로. in-chunks.ts 헬퍼 있음.
- 결제 시 round2_unlocked 심기 금지 (fc64a22) — R1 완료 표시가 깨짐.
- 공유 링크는 날짜 버전 자동 부착 (5d1641c) — 카톡 미리보기 캐시 자가 치유.

---

# 운영 안정화·현장 대응 로그 (2026-07-30 ~ 08-09)

## 보안 — 내신 관리 올라영 전용 잠금 (110224a + 103 마이그레이션)
- 배경: 올킬보카 외부 가입 선생님이 /admin/naesin에서 내신 콘텐츠 전체 열람 + RLS상 수정 가능했음.
- `academies.naesin_enabled` 스위치 (기본 false, 올라영만 true). **외부 판매 시작 시 해당
  학원 스위치만 켜면 됨** (그때 boss 토글 UI 추가 예정).
- 잠금: 사이드바(requireNaesin) + 페이지 리다이렉트 + /api/naesin/* teacher/admin 자동 검사
  (createApiHandler 경로 기반) + RLS 쓰기 naesin_manage_allowed().
- **익명 읽기 구멍 봉인**: problem_sheets 등 5테이블이 USING(true)라 로그인 없이 anon key로
  전체 덤프 가능했음 → 인증 필수로. 학생 무료 체험(내신 1단원)은 의도된 기능이라 읽기 유지.

## 보카 채점·시험 신뢰성 (수업 현장 발견 → 당일 수정 사이클)
| 사건 | 원인 | 재발 방지 |
|------|------|-----------|
| 전하라 반의어 오답 처리 | 아이폰 곱슬 아포스트로피(U+2019) | **normalize-typed.ts** — 보카 타이핑 채점 7지점 공용 (aebb483) |
| 배정 목록 "day 21,21,22" | voca_days (book,day_number) 중복 방지 없음 | days POST 409 + 업로드 연타 방지 + 관리·배정 화면 중복 배지 (485a619) |
| 김지민 circuit(안 배운 단어가 시험에) | 올킬시험 기본 교재 = books[0] + 전체 교재 노출 | 시험 교재 = 배정+진도 교재만, 기본 = 배정→최근 학습 (2c98ac5) |
| 김선우 90점 증발 | 합격 시 "다음 단계로" 클릭까지 저장 지연 | **onScoreFinal** — 점수 확정 즉시 저장, 클릭은 화면 이동만 (72180bb) |
| 2회독 점수 화면 불일치 | Day별 결과가 round1만 조회 | Day별 결과 "2회독" 열 추가 (5e76f2d) |
| 박태욱 진단 기록 실종 | 재원생이 공개 /level-test로 응시 → 리드함 저장 | 로그인 학생은 학생용 진단으로 자동 이동 가드 (a71cba5) |

- ⚠️ **후속 검토**: Day 학습 스펠링(neon/index)도 합격 시 onComplete 지연 패턴 — 진도 저장이라
  유실 비용 낮아 미수정. 해당 화면 만질 때 onScoreFinal 패턴 적용할 것.
- 2회독 유의어·반의어 시험은 원래 즉시 저장이라 안전 (수정 불필요였음).

## 올킬시험 배정 학생-우선 재설계 (9c5e5fa)
- 현장 피드백 3건: 학생별 교재를 외워야 함 / 타학원(개인) 학생 노출 / 교재 배정 기능 발견 불가.
- 플로우 역전: 학생 선택(검색) → 배정 교재로 자동 그룹핑 → 그룹별 Day 선택 (교재 섞여도
  자동 분리 배정). 학생 칩에 교재·진도, 그룹에 최근 시험 힌트. 미배정 학생 인라인 교재 배정.
- API: boss 무학원 시 개인 가입자 제외. teacher/admin/boss 공용 컴포넌트.

## 스태프 커뮤니케이션·세션
- 업데이트·사용법(updates.ts)에 **popup 플래그** — true인 최신 항목을 스태프 로그인 시
  1회 모달 (e34eb9d). 사용 방식이 바뀌는 중요 개편에만 켤 것 (남발 금지).
- **3시간 무활동 자동 로그아웃** 전 역할 (f569892) — localStorage 활동 추적, 탭 간 공유.

## boss 상비 진단 도구 (브라우저로 여는 조회 API)
- `/api/boss/student-lookup?name=이름` — 학생의 진단(실서비스와 동일한 추천 계산)·배정 교재·
  진도·시험 기록 + **레벨테스트 리드(이름 일치+최근 24h)** 한 번에. 동명이인 전부 표시.
- `/api/boss/voca-recent-words?hours=48` — 최근 추가된 Day/단어를 위치별로.
- 조사 원칙: 학생 제보는 먼저 이 도구로 데이터 확정 → 코드 추적. "기억"과 DB가 다르면
  대부분 별도 화면/계정/저장 실패 (김선우·박태욱 사례).

---

# Claude 작업 환경 메모

- 이 환경은 네트워크 `*.supabase.co` + 실서비스 도메인 허용, Supabase 자격증명 환경변수 등록됨
  (2026-08-09 설정) → **새 세션부터 DB 직접 조회 가능**. 안 되면 위 boss 진단 API 우회.
- Setup script가 npm install 수행 — 세션 시작 시 의존성 준비됨.
- 검증 루틴: `npx tsc --noEmit` + `npx vitest run` (991+) + 변경 파일 eslint. 배포는 main 머지
  (Vercel 자동). 마이그레이션은 supabase/migrations에 추가하면 production 빌드가 자동 적용.
